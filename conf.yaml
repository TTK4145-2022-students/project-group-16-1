# Elevator server 
elevator_server:
  host: host
  port: 15657

# Elevator 
elevator:
  N_floors: 4
  N_btn_types: 3

# Elevator control 
elevator_control:
  ticker_interval: 50 #Miliseconds

# Order redundancy
order_redundancy:
  ticker_interval: 50 #Miliseconds


